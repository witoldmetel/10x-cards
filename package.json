{
  "name": "10x-cards",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "Client",
    "API/TenXCards.API"
  ],
  "scripts": {
    "client": "cd Client && npm run dev",
    "server": "docker compose up",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
